# Mac stuff, ignore
.DS_Store

# ignore dev/ folder
dev/

# Eclipse stuff, should be for each developer, so, ignore
.classpath
.project
.settings
# since we use Maven, ant buil.xml will be private for each developers
#  (if they need their own scripts to do some stuff)
build.xml
# maven target (we definitely do not want this one in git)
target/
# prevent tmp/ should be for dev db and stuff
tmp/

node_modules/

# prevent the compiled files
src/main/webapp/workbench/js/controllers.js
templates.js
*.css
!src/main/webapp/bootstrap/css/*
!src/main/webapp/canvassdk/css/*
!src/main/webapp/hack/*.css


# intellij and sublime stuff
*.iml
.idea/
*.sublime-project
*.sublime-workspace